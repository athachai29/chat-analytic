name: Deploy to App Engine
on:
  push:
    branches:
      - main

jobs:
  echo_secret:
    runs-on: ubuntu-latest

    steps:
      - name: Echo Secret Variable
        run: echo ${{ secrets.GCLOUD_SERVICE_ACCOUNT_KEY }} && echo something && echo ${{ secrets.GCLOUD_PROJECT_ID }}
